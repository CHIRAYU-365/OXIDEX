{
  "name": "oxidex-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "blockchain",
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install",
    "build:frontend": "cd frontend && npm run build",
    "deploy:frontend": "cd frontend && npm run deploy",
    "start:backend": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "gh-pages": "^6.3.0",
    "wait-on": "^7.2.0"
  }
}
